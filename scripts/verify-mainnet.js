require('dotenv').config();
const hre = require('hardhat');

const main = async () => {
}

main()
  .then(() => process.exit(0))
  .catch(error => {
    console.error(error);
    process.exit(1);
  });
